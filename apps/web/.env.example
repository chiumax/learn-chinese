# Server-side Postgres connection for the sync backend.
# Optional for now — the app runs fully offline/local without it.
DATABASE_URL=

# Public app name (optional).
NEXT_PUBLIC_APP_NAME=learn-chinese
